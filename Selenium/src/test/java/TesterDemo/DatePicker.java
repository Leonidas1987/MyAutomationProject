package TesterDemo;

import org.openqa.selenium.By;
import org.openqa.selenium.WebElement;

import java.util.List;

public class DatePicker {
    /* driver.findElement(By.id("datepicker")).click(); // Выбираем поле даты
    WebElement dataWidget = driver.findElement(By.id("ui-datepicker-div"));  // Выбираем сам календарь
    List<WebElement> cells = dateWidget.findElements(By.tagName("td")));  // Уменьшаем поиск таблиц
for(WebElement cell: cells) {
    if(cell.getText().equals("18")) {
        cell.click();
        break;
    }
    } */
}
